<!-- changing state — read & written every run; never moved into SKILL.md. -->

# {{LOOP_NAME}} — State Ledger

> This file is the loop's memory. Every run reads it at start and writes it
> before stopping. Do not embed it in the loop's `SKILL.md` — a skill is
> reloaded from disk on each cold start, so any state stored there silently
> resets. This file persists between runs precisely because it is external.

---

## Ledger

| group | item | status | notes |
|-------|------|--------|-------|
| default | (initial) | pending | scaffold — no runs yet |

<!--
Add one row per work item as the loop discovers and processes them.
Valid status values: pending · in-progress · done · failed · skipped
The `group` column is how the loop records its decomposition of the task set:
one group for a single-workstream loop, one per sub-loop / repo / phase when the
loop fans a task set into parallel stacks. The dashboard renders one block per
distinct group, so the board's shape follows the loop's own decomposition.
(A legacy 3-column ledger without `group` still renders — everything lands in a
single "default" group.)

Optional richer columns — add any of these to get informative line items on the
dashboard (a code-shipping loop typically fills them; a plain loop omits them):

| group | item | status | ref | type | branch | pr | notes |
|-------|------|--------|-----|------|--------|----|-------|
| caterspot_admin_app | Tax-group selector on 4 forms | done | ADMIN-1 · CAT-9901 | NEW | fm/cat-9621 → fm/cat-9901 | #1823 | |

  - `ref`    — two-line ticket cell, split on `·` or `/` (part above, id below)
  - `type`   — NEW / REUSE tag (NEW is accent-colored)
  - `branch` — base → head, shown under the title; the stack's base branch is
               derived from the first row's left side
  - `pr`     — `#1823` (chip) or a full URL (linked)
  - `notes`  — inline note under the title; contains "TBC"/"!" → red, else muted
-->

---

## Last run

<!-- Update this block at the end of every run, including failed runs. -->

```
timestamp : (not yet run)
iteration : 0
outcome   : —
exit code : —
```

---

## Notes

- Keep rows here as the loop runs; do not delete completed rows (they form an
  audit trail).
- If a row reaches `failed`, the loop must halt and a human gate must be
  cleared before the next run (see `HUMAN-GATES.md`).
- Goal predicate: **{{GOAL}}**
- State path for this loop: `{{STATE_PATH}}`
